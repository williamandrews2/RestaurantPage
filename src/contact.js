export function loadContact() {
  const content = document.getElementById("content");

  // Add test text to page:
  const newText = `<h1>This is testing the contact page</h1>`;
  content.innerHTML += newText;
}
